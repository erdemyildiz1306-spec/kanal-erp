{
  "name": "kanal-erp-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android && node scripts/patch-android.js",
    "open": "npx cap open android",
    "patch": "node scripts/patch-android.js",
    "build:apk": "npm run sync && cd android && gradlew.bat assembleDebug && cd .. && node scripts/copy-apk.js"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/app": "^7.0.1",
    "@capacitor/camera": "^7.0.1",
    "@capacitor/push-notifications": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
